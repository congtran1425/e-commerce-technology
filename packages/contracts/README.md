# API contracts

Kiểu request/response và enum được frontend và backend dùng chung.

Contract trong package này phải đồng bộ với `docs/api/openapi.yaml`. Không đặt business logic hoặc code phụ thuộc React/Express tại đây.
